import { createRoutePath } from "@/core/utilities";

const {{moduleName.pascal}}RoutePaths = createRoutePath({
  Index: "{{moduleName.camel}}",
} as const);

export default {{moduleName.pascal}}RoutePaths;
